#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

int main(){

  int lambda;
  int N; // numero de canais 
  double c;
  double u ;

  printf("Enter the number of lambda:");
  scanf("%d",&lambda);
  u = (rand()+1)/RAND_MAX;
  printf("valor de u : %d" , u);
  //c = -(1/lambda)*ln(u);
  //printf("valor de c: %lf\n", c );



}